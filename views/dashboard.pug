doctype
html
  head
  body
    h1 Welcome
    a(
    href 
      = sails.getUrlFor('UserController.settings') 
     ) settings
     
     a(
     href 
       = sails.getUrlFor('UserController.logout') 
      ) logout
     
     
